services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: worknearby
      POSTGRES_USER: worknearby
      POSTGRES_PASSWORD: worknearby
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

